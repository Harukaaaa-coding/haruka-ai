# Install PyTorch separately with the CUDA wheel appropriate for the cloud GPU.
# training/scripts/bootstrap_cuda.sh does this before installing this file.
transformers>=4.51.0
datasets>=3.5.0
accelerate>=1.6.0
peft>=0.15.0
trl>=0.17.0
bitsandbytes>=0.45.5; platform_system != "Windows"
safetensors>=0.5.3
sentencepiece>=0.2.0
